#include <bits/stdc++.h>
#include <sys/stat.h>
#include <dirent.h>
#define MAXN 10000

using namespace std;

#include "custom_git.h"

int main() {

    tree root;
    root = init_tree(".", ".");
    tree_display(root);

    return 0;
}
